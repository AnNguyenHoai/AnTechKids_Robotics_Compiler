# Portability Field Semantics

The `distribution-manifest.json` `portable` field remains `false` under the established RSD-17 contract.

RSD-23 changes ownership and packaging of Python/PlatformIO, but does not redefine this manifest field. Runtime ownership is represented by the bundled runtime layout and deployment manifest.
