# B2.3 regression gate

Run `python tests/b2_3/run_b2_3.py`.

The gate verifies relocation-safe application/state paths, immutable release payload, external writable state, PlatformIO mutable/immutable directory split, hostile override rejection, final subprocess state preservation, Unicode/space paths, unrelated CWD operation, and fail-fast invalid state roots.
