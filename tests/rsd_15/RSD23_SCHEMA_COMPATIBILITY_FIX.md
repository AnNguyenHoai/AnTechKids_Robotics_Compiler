# RSD-15 compatibility regression fix

RSD-23 adds application-owned runtime payload without changing the existing distribution manifest schema. The locked distribution schema remains version 2; runtime ownership changes are represented by the existing manifest fields and the deployment/runtime contracts.
